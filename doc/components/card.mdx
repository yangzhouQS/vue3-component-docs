---
title: Card 卡片
description: 通用信息容器卡片。
---

# Card 卡片

用于将信息分组展示的容器组件，支持 Less 自定义样式。

```vue file="./_demo-card.vue" preview="iframe-follow"

```

## 属性

| 参数 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| header | 卡片标题 | `string` | `-` |
| shadow | 阴影显示方式 | `'always' \| 'hover' \| 'never'` | `'always'` |
